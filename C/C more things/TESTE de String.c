#include <stdio.h>
int main(){
    int i = 0, aux = 0,j = 0, casos = 1c;
    char codigo[i][j];
    for(j = 0; j < casos; j++)
        fgets(codigo[j], 1000, stdin);
        char frase



return 0;
}
